{
  "name": "locus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drop a pin and discover the historical events closest to that place.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
